/*$(document).ready(function (e){
     
        function Confirm(title, msg, $true, $false) { /*change
            var $content =  "<div class='dialog-ovelay'>" +
                            "<div class='dialog'><header>" +
                            " <h3> " + title + " </h3> " +
                            "<i class='fa fa-close'></i>" +
                        "</header>" +
                        "<div class='dialog-msg'>" +
                            " <p> " + msg + " </p> " +
                        "</div>" +
                        "<footer>" +
                            "<div class='controls'>" +
                                " <button class='button btn green doAction'>" + $true + "</button> " +
                                " <button class='button btn btn-default cancelAction'>" + $false + "</button> " +
                            "</div>" +
                        "</footer>" +
                    "</div>" +
                    "</div>";
            $('body').prepend($content);
            $('.doAction').click(function () {
                $(this).parents('.dialog-ovelay').fadeOut(500, function () {
                    return true;
                $(this).remove();
                });
            });
            $('.cancelAction, .fa-close').click(function () {
                $(this).parents('.dialog-ovelay').fadeOut(500, function () {
                    return false;
                $(this).remove();
                });
            });
        
        }


        /**Affichage de la boite de dialogue */

        /*$('.formSubmit').submit(function (e){
            
            return Confirm('MA2E', 'Êtes vous sûr de continuer l\'opération?', 'Oui', 'Annuler');
            return false;

        });*/



//});*/